import { Directive } from '@angular/core';

@Directive({selector: 'i18n-country-select[iso3166Alpha2]'})
export class Iso3166Alpha2Directive {
  // No behavior
  // The only purpose is to "declare" the tag in Angular2
}
